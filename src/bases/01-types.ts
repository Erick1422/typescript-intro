
// Cuando se trata de constantes el tipo de dato se vuelve opcional porque ts ya sabe su valor.
export let name = 'Erick';
export const age = 20;
export const isValid: boolean = true;

name = 'xd';

export const templateString = ``;